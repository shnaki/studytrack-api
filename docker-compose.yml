services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: studytrack
      POSTGRES_USER: studytrack
      POSTGRES_PASSWORD: studytrack
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U studytrack"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
